import{describe,expect,it}from"vitest";import{budgetAllows,retryDelay,triggerMatches,withinQuietHours}from"./dani-policy.ts";describe("Dani policy",()=>{it("matches typed triggers",()=>expect(triggerMatches({id:"1",eventType:"mail",predicate:{sender:"a"},dedupeWindowMs:1,enabled:true},{type:"mail",payload:{sender:"a"}})).toBe(true));it("handles overnight quiet hours",()=>expect(withinQuietHours(new Date("2026-09-20T19:00:00Z"),{timezone:"Asia/Calcutta",start:"22:00",end:"07:00"})).toBe(true));it("enforces budgets and bounded retries",()=>{expect(budgetAllows({turns:1,toolCalls:2,costUsd:3},{turns:1,toolCalls:0,costUsd:0})).toEqual({ok:false,reason:"turns"});expect(retryDelay(20)).toBe(60000)})});
