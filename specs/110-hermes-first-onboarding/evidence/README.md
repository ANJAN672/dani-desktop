# Evidence index

No acceptance evidence has landed. Add immutable commit/tree, command or run URL, artifact hash/version, target OS/device, verifier, timestamp, failure links, and waiver expiry where applicable. Never commit secrets or large unredacted logs.
