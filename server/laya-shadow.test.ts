import{afterEach,describe,expect,it}from"vitest";import{mkdtempSync,rmSync}from"node:fs";import{join}from"node:path";import{tmpdir}from"node:os";import{LayaShadow}from"./laya-shadow.ts";const roots:string[]=[];afterEach(()=>roots.splice(0).forEach(r=>rmSync(r,{recursive:true,force:true})));describe("Laya shadow",()=>{it("never changes Hermes execution choice",async()=>{const root=mkdtempSync(join(tmpdir(),"laya-"));roots.push(root);const l=new LayaShadow(join(root,"l.db"),{score:async()=>[{candidateId:"b",score:1,reason:"shadow"}]});const r=await l.observe("x",[{id:"a",label:"A",features:{}},{id:"b",label:"B",features:{}}],"a");expect(r.executionChoice).toBe("a");expect(r.shadowChoice).toBe("b");expect((l.metrics()as any).runs).toBe(1);l.close()})});
